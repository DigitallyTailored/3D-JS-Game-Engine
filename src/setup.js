'use strict';

var COIN = uNum();
var END = uNum();
var levelNumber = 0; //calling nextLevel to set to increment
var gemCount = 0; //calling nextLevel to set to increment
//setup all game specific stuffs
var player = new Character();
